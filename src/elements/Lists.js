import styled from 'styled-components';
import { teal } from '../utilities';

export const GuideList = styled.ul`
        color: ${teal};
        font-size: 0.8rem;
        text-align: left;
        width: 25%;
        position: absolute;
        top: 3%;
`;
